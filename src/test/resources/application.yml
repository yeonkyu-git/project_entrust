spring:
  profiles:
    active: local
#
#  datasource:
#    url: jdbc:h2:tcp://localhost/~/h2database/entrust
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        default_batch_fetch_size: 500
        #        show_sql: true
        format_sql: true
  #        use_sql_comments: true
  messages:
    basename: errors

logging.level:
  p6spy: info
  org.hibernate.SQL: debug
#  org.hibernate.type: trace

file:
  dir: /Users/yeonkyu/Desktop/Projects/Spring/03.entrust/entrust/images